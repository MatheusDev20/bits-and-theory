### Book

High performance broswe network

[x] Understand a concept deeply


### Concepts

TCP is a layer of abstraction in the network
Guarantee In order delivery, retransmission of lost data, congestin control, data integrity
TCP Stream -> "Every byte sent will be identical with the byte received in the same order"


TCP connections start with a concept called "Three way handshake" 
It performs a full roundtrip betwen client and server in the application layer, so it is "latency expensive"

So reuse TCP connections is important for applications running over TCP
    [?] Need an actual example of this reuse, this is done automatically by HTTP? code can demonstrate this?



