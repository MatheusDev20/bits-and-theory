
# Lambda to make anonymus procedures.
(define square (lambda (x) (* x x)))

(define (compose f g)
    (lambda (x) (f (g x)))
)

(define (roots a b c)
    (let ((d sqrt (- (* b b) (* 4 a c)))))
        (-b (-b))
        (2a ( * 2a))
    (se (/(+ -b d) 2a))
        (/(- -b d)2 a)
)